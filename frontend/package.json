{
  "name": "@dod/web",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "dod's web UI (Lit + TypeScript). Built dev-time; the bundle is committed into ../src/dod/web and served statically by the stdlib-only Python daemon.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && BUILD_TARGET=app vite build",
    "build:web": "npm run build && cp dist/dashkit.js dist/app.js ../src/dod/web/",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "lit": "^3.1.0"
  },
  "devDependencies": {
    "jsdom": "^25.0.0",
    "typescript": "^5.6.0",
    "vite": "^8.1.0",
    "vitest": "^4.1.9"
  }
}
